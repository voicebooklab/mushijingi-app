# カード管理アプリ v1 進捗まとめ（2026-09-06時点）

## 今できること
- カードの手入力登録（カード名／コスト／体力／色／攻撃1・効果1(任意)・攻撃2(任意)・効果2(任意)／レア度／第何弾）
- カード一覧・名前検索・弾での絞り込み
- 弾ごとの登録進捗表示（登録数／全枚数／残り枚数）
- カード詳細画面：攻略メモ5項目（良かった使い方／失敗した使い方／相性が良かったカード／苦手だったカード・注意点／その他メモ）を保存・閲覧
- カード詳細から「このカードを使った大会の履歴」を自動表示（既存の大会・デッキデータを再利用、二重入力なし）
- デッキ作成（新規用の短い画面／過去デッキ登録用の詳しい画面の2種類）
- 同名デッキは上書きせず別バージョンとして保存・一覧できる
- デッキ比較（2つ選んで、共通カード・抜いたカード・追加したカード・枚数増減を表示。保存はしない）
- 大会・戦績記録、大会ごとの各試合記録（勝敗／相手／相手の特徴／良かった点／改善点／メモ、何試合でも追加可）
- 音声入力（🎤ボタン）：ほぼ全部のテキスト入力欄に対応（Android ChromeのWeb Speech API、追加費用なし）

## 保存場所・起動方法
- フォルダ：`C:\Users\wilch\Downloads\mushijingi-app`
- 起動：このフォルダで `npm start`
- パソコンから：`http://localhost:3000`
- スマホから：同じWi-Fiで `http://192.168.1.4:3000`（IPは変わることがあるので、繋がらない時は要確認）
- データ：`data/data.sqlite` に保存。バックアップは今のところ手動（このファイルをコピーするだけでOK）

## 今回あえて入れていないもの
- 写真の撮影・保存
- OCR（写真からの自動読み取り）
- AI相談機能
- CPU対戦
- 複数ユーザー機能（ただしデータ構造はuser_idで分離済みなので、将来追加しやすい設計）

## 分かっている制約
1. **24時間いつでも使えるわけではない。** パソコンが起動していて`npm start`が動いている時だけ使える。パソコンを閉じるとスマホからもアクセス不可。→ 常時起動にするか、クラウドに置くかは未決定（本人判断待ち）。
2. **スマホの音声入力は、Chromeの設定変更が1回必要になる可能性が高い。** 今の接続方法（`http://192.168.1.4:3000`）はhttpsでもlocalhostでもないため、Chromeがマイクをブロックすることがある。対処法は`chrome://flags/#unsafely-treat-insecure-origin-as-secure`にこのURLを登録すること（無料・1回だけ）。

## 次にやるとしたら（本人の意向）
- **写真機能への着手**（カード写真の撮影・保存）。その先にOCR自動読み取りも視野。

## データ設計（参考）
- `users` / `sets` / `cards` / `card_images`（未使用・将来用に予約）/ `decks` / `deck_cards` / `tournaments` / `tournament_matches`
- 詳しいテーブル定義は [db.js](db.js) を参照
