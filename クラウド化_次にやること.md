# クラウド化：次にやること（本人確認用）

## 今のところまで（Claudeが済ませたこと）
- アプリの中身を、クラウド（インターネット上のどこからでも繋がる場所）に置けるように作り替えた
- パソコンでの今まで通りの使い方（`起動.bat`をダブルクリック）は、**何も変えず動作確認済み**。実データ（カード23枚・デッキ1個）も無事
- あとは、**無料のアカウントを2つ作っていただくだけ**で、私がその先を全部やります

## お詫び
前回「アカウント登録は1回だけ」とお伝えしましたが、調べ直した結果、**正しくは2回**でした。訂正します。

## 作っていただきたい無料アカウント（2つ）

いずれも**クレジットカードの登録は不要**です。メールアドレスだけで作れます。

### ① Turso（データを保存しておく場所）
- サイト：https://turso.tech/
- 「Sign Up」からメールアドレスで登録するだけ

### ② Render（アプリを24時間動かしておく場所）
- サイト：https://render.com/
- 「Get Started」からメールアドレスで登録するだけ

## 作ったら
「①と②、両方作りました」とだけ教えてください。**あとの設定作業（データの引っ越し・アプリの設置）は、全部私がやります。**スマホでの操作は発生しません。

---

## 技術メモ（Claude用・読まなくて大丈夫です）
- データ保存を `node:sqlite` から `@libsql/client` に移行済み（db.js / server.js は async化）
- ローカルは `file:` URL、クラウドは `TURSO_DATABASE_URL` / `TURSO_AUTH_TOKEN` の環境変数で自動切替
- 動作確認済み：カード登録（虫/術/強化）・攻略メモ・デッキ作成（トランザクション）・デッキ比較・大会/試合記録、すべて実データで確認
- バックアップ：`db.js.bak_before_libsql`, `server.js.bak_before_libsql`, `data/data.sqlite.bak_before_libsql_20260911_153324`
- Render用のデプロイ設定はアカウント取得後に作成する（Render側の実際のUIを見てから確定させる方が確実なため、今夜は準備のみ）
